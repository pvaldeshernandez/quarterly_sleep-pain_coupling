# Function registry

Every callable in the pipeline, as a tree: file, then its functions, then
anything nested inside them. Consult it BEFORE writing a new function —
if the concept is already here, reuse or generalize it rather than adding
a second copy.

Generated by `tools/make_functions_md.py` from the docstrings themselves.
Do not edit by hand; run the tool.

## Shared library — `lib/`

### `analytic_sample.py`

Who is in the analytic sample.

- `analytic_ids(source, id_col)` — The participant IDs of the analytic sample, as a set of strings.

### `baseline_stability.py`

Stability of a repeated measure's association with a person-level baseline variable.

- `correlate(x, y, min_pairs, method)` — Correlation r/rho, p and n over the IDs where both Series are finite.
- `per_occasion(repeated, baseline, occasions)` — Correlate a person-level `baseline` Series with `repeated` at each occasion.
- `against_person_mean(repeated, baseline, method)` — Correlate `baseline` with each person's MEAN of the repeated measure.

### `coupling_model.py`

Bayesian VARX(1) Model for Bidirectional Sleep-Pain Coupling

- `_fmt(x)` — JSON-safe scalar.
- `write_diagnostics(idata, fit_id, cfg, out_dir)` — Record how well one fit sampled, beside the fit itself.
- `read_diagnostics(out_dir, fit_id_prefix)` — Every diagnostics record `write_diagnostics` left in `out_dir`, as one frame.
- `summarize_posterior(idata, labels, n_obs, n_persons, var_names)` — One tidy row per scalar parameter of one fit.
- `run_fit(model, fit_id, out_dir, progressbar, **overrides)` — Sample `model` with the project's settings and record how it went.
- `load_data(data_dir, synthetic)` — Load processed quarterly data with within-person decomposition.
- `two_tail_p(prob_neg)` — Two-tailed Bayesian posterior "p-value" from P(parameter < 0).
- `sign_concordance_p(n_concordant, n_tested)` — Upper-tail binomial p-value for observing ``n_concordant`` out
- `load_varx_frame(csv_path, verbose)` — Load the VARX-ready long dataframe and prepare it for any
- `add_bivariate_innovations_likelihood(mu_pain, mu_sleep, y_pain, y_sleep, name_suffix)` — Attach the bivariate-normal innovations likelihood to the current
- `build_timevarying(model_df, df_full, raw_long, covars, standardize, binary, interpolate)` — Within-person-centered time-varying covariates and their within-segment lag-1.
- `fit_bayesian_varx1(model_df, unique_ids, id_map, X_person, include_agesex, include_sp, include_ps, moderator_direction, X_tv, idata_kwargs, cores, progressbar, fit_id, out_dir)` — Fit the Bayesian VARX(1) model with contrast and optional moderators.
- `fit_roi_moderation_set(roi_df, model_df, unique_ids, id_map, direction, rois, fit_id_prefix, out_dir, include_agesex, progressbar, verbose)` — Fit one moderation model per ROI and tabulate them.
- `compute_loo_comparison(data_dir, synthetic)` — Fit four nested coupling-direction models and compare via LOO-CV.
- `compute_jn_curve(intercept_draws, slope_draws, X_vals, raw_mean, raw_sd, clip_pct)` — Compute a Bayesian Johnson-Neyman (JN) significance curve.
- `extract_results(trace, moderator_name)` — Extract summary statistics from the posterior.
- `extract_person_posteriors(idata, unique_ids, direction)` — Extract person-specific coupling posterior summaries.

### `descriptives.py`

Item-level descriptives and the canonical item lists.

- `item_descriptives(df, items, scale, floor, ceiling, region_map, sample)` — Per-item descriptives on whatever rows the caller passes.
- `icc_varcomp(df, value, group)` — One-way random-effects ICC from VARIANCE COMPONENTS, not a variance ratio.
- `restrict_to_analytic(raw, analytic, by)` — The rows of ``raw`` whose ``by`` key appears in ``analytic``.

### `heatmap.py`

The stability heatmap: one cell per (measure, occasion), carrying r, p and n.

- `fmt_p(p)` — p as printed in a cell: scientific below .001, three decimals above.
- `render(r, p, n, columns, row_labels, out_path, alpha, vmin, vmax, cbar_label, dpi)` — Draw the grid and save it.

### `measurement.py`

Measurement-model comparison helpers.

- `tucker_congruence(x, y)` — Tucker's coefficient of congruence between two loading vectors.

### `nuisance.py`

Person-level nuisance covariates: reading them, aligning them, removing them.

- `read_spm_design(path)` — The design matrix of an SPM.mat, as a float array (n_volumes, n_columns).
- `fd_from_design(X, n_motion, radius_mm)` — Framewise displacement per volume, from the motion columns of a design matrix.
- `person_covariate(path, column, id_columns)` — ``{ID: float}`` for one column of a wide, one-row-per-participant table.
- `roi_moderator(roi_df, roi, value_col)` — ``{ID: z_value}`` for one ROI of a long ROI-value frame.
- `align(ids, *maps)` — Look several person-level maps up on one ID list.
- `residualize(ids, values, covmaps)` — Regress a moderator on one or more person-level covariates; z-score the residual.
- `cohens_d(a, b)` — Standardized mean difference with the pooled SD (ddof=1 in each group).

### `ppc.py`

Posterior predictive simulation for the VARX(1) coupling model.

- `mean(x)` — *(undocumented)*
- `sd(x)` — *(undocumented)*
- `q05(x)` — *(undocumented)*
- `q95(x)` — *(undocumented)*
- `iqr(x)` — *(undocumented)*
- `kurt(x)` — Excess kurtosis (0 for a Gaussian). a02_diagnostics.py:201.
- `simulate_replicates(idata, md, n_rep, rng, innovations, return_mu)` — Draw y_rep from the posterior predictive by re-implementing the model.
- `ppc_table(idata, md, n_rep, seed, innovations, return_arrays)` — Full posterior predictive check table for one fit.
- `_toy_idata_and_frame(seed, n_obs, n_persons, n_chains, n_draws)` — A tiny fake posterior + model frame. No fitting, no data touched.
- `verify_gaussian_identity(verbose)` — max |delta| between this module's Gaussian path and a02's must be 0.
- `verify_studentt_moments(verbose)` — The Student-t path must inflate variance by nu/(nu-2) and keep rho.

### `registry.py`

The numbers registry — every quantity a step computes, under a stable key.

- `_plain(v)` — Convert numpy scalars and arrays to JSON-safe Python values.
- `write_numbers(step_dir, mapping, prefix)` — Write `mapping` to `<step_dir>/numbers.json`, replacing any previous content.
- `read_all(results_root)` — Merge every step's `numbers.json` under `results_root` into one dict.

### `sleep_instruments.py`

Canonical registry of the baseline sleep instruments used in the R1.1/R2.3 revision.

- `instruments()` — Variable names of the sixteen sleep instruments, in registry order.
- `label(varname)` — Publication label for a variable name; falls back to the raw name.
- `labels_for(varnames)` — Publication labels for a sequence of variable names.
- `is_pain_item(varname)` — True for the diary items that measure pain rather than sleep.
- `select(df)` — Restrict a variable-name-indexed frame to the sixteen sleep instruments.
- `diary_stems(dd, form, n_nights)` — Every item of `form` that has exactly `n_nights` nightly variables.
- `baseline_frame(wide, dd, min_nights)` — Every column-based baseline sleep measure as person-level values, plus metadata.

### `stopbang.py`

Canonical STOP-BANG obstructive sleep apnea risk score.

- `_yes(series)` — 1.0 for an endorsed binary item, 0.0 for a denied one, NaN when absent.
- `components(wide)` — Per-participant STOP-BANG components and total, indexed by ID.
- `_default_ids()` — The analytic sample, for callers that pass no explicit ID set.
- `scores(ids, wide)` — STOP-BANG total per participant, restricted to `ids` and to complete scorers.
- `summary(q13_person_mean, ids, wide)` — Every STOP-BANG figure reported in the paper, in one dict.
- `test_reproduces_published(q13_person_mean, ids, wide)` — Assert this module reproduces the numbers Section S4 and the Response quote.

## Pipeline steps — `codes/python/`

### `generate_all_results.py`

Regenerate all manuscript results from saved derivatives — no model refitting.

- `main()` — *(undocumented)*

### `run_pipeline.py`

Run the whole pipeline, in order, from a terminal.

- `discover()` — [(number, module_name)] in execution order.
- `entry(mod, number)` — The step's callable, and whether it accepts `refit`.
- `main()` — *(undocumented)*

### `step00_extract_data.py`

Step 00 — Extract paper-relevant variables from the legacy wide-format data.

- `reshape_quarterly_items(wide)` — Reshape ``qhu{quarter}_q{item}__s1`` columns to long format.
- `apply_gateway_imputation(long)` — Apply gateway-question imputation to quarter 1..11 rows.
- `build_baseline_rows(wide)` — Build the ``quarter = 0`` rows carrying baseline variables.
- `assemble_long(wide)` — Assemble the full long-format output.
- `filter_dictionary(dict_df)` — Keep dictionary rows that correspond to extracted variables.
- `extract(verbose, refit)` — Run the full extraction and write outputs.
- `main()` — *(undocumented)*

### `step01_factor_analysis.py`

Step 01 — Factor analysis and interpolation on the extracted long data.

- `polychoric_corr_pair(x, y)` — Estimate the polychoric correlation between two ordinal variables.
  - `neg_loglik(rho_arr)` — *(undocumented)*
- `polychoric_corr_matrix(data, items)` — Compute the full polychoric correlation matrix for ordinal items.
- `calibrate_factor_model(quarterly_items, use_polychoric, verbose)` — Iterative principal axis factoring with 2 factors.
- `run_parallel_analysis(n_obs, n_vars, n_replications, seed)` — Horn's (1965) parallel analysis for factor-retention decisions.
- `report_parallel_analysis(actual_eigs, pa_results)` — Print the parallel-analysis comparison table.
- `bartlett_scores_2f(df, model)` — Compute 2-factor Bartlett scores per row, handling partial items.
- `score_all_rows(long, model)` — Score pain factor, contrast factor, and z-sleep on every row.
- `interpolate_factor_scores(long)` — Linearly interpolate single interior gaps in factor scores.
- `run_step01(verbose, refit)` — Full Step 01 pipeline: load, factor-analyze, score, interpolate.
  - `_r(metric, value, note)` — *(undocumented)*
- `main()` — *(undocumented)*

### `step02_measurement_checks.py`

Step 02 — Measurement checks on the published two-factor pain solution.

- `load_published_solution(path)` — The published two-factor solution, loaded rather than re-estimated.
- `calibration_sample(long)` — Complete-case quarterly rows on all eight pain items.
- `build_samplings(df)` — Four samplings of the same eight items, each as a DataFrame.
- `align_loadings(model, target_items)` — Reorder a fitted loading matrix into `target_items` order.
- `estimate_two_factor(sampling, frame, target_items)` — Two-factor solution for one sampling, through the canonical estimator.
- `_dominance_row(level, diff)` — Descriptives of one knee-minus-body difference distribution.
- `knee_body_dominance(df)` — Knee-mean minus body-mean, per person-quarter and per participant.
- `compute(verbose)` — Re-estimate the four solutions and build the four tables.
- `assemble_numbers(loadings_df, congruence_df, dominance_df)` — Every scalar Section S3 and Table S2 quote, derived from the saved tables.
- `run_step02(verbose, refit)` — Measurement checks on the two-factor solution (Table S2, Section S3).
- `main()` — *(undocumented)*

### `step03_contrast_validation.py`

Step 02 — External validation of the contrast factor.

- `_load_person_mean_contrast()` — Return DataFrame with columns [ID, K_i] from step01 scored output.
- `generate_figure_s1(verbose)` — Figure S1: body-map endorsement point-biserial correlations.
- `generate_figure_s2(verbose)` — Figure S2: convergent validity of the contrast factor vs baseline
- `generate_text_numbers(verbose)` — Compute and save all convergent validity statistics to
  - `_t(metric, value, note)` — *(undocumented)*
- `run_step03(verbose, refit)` — *(undocumented)*
- `main()` — *(undocumented)*

### `step04_prepare_varx_data.py`

Step 03 — Segment filter, within-between decomposition, lag creation,

- `segment_filter(df)` — Identify and retain segments of >= MIN_SEGMENT consecutive quarters.
- `decompose_within_between(df)` — Decompose each factor score into between- and within-person parts.
- `create_lagged_variables(df)` — Create t-1 lags and VARX interaction terms within segments.
- `compute_timepoint_summary(df)` — Compute the timepoint summary matching manuscript claims.
- `compute_table3(df_processed, df_full)` — Compute Table 3 demographics for the analytic sample.
  - `_add(variable, level, value)` — *(undocumented)*
- `generate_figure1(df_processed, df_full, out_path)` — Generate Figure 1: data availability grid.
- `run_step04(verbose, refit)` — Full Step 03 pipeline.
- `main()` — *(undocumented)*

### `step05_raw_descriptives.py`

Step 05 — Raw quarterly descriptive statistics.

- `load_frames(verbose)` — Return (raw_long, processed) as (DataFrame, DataFrame), IDs typed as str.
- `_compute(verbose)` — Everything this step knows. Returns (frames, numbers, suppressed).
- `_numbers(frames, n_participants, n_described, n_by_id, n_by_pair, n_retained, n_suppressed, pain_items)` — Every scalar the documents quote, or could quote, from this step.
- `_write(frames, numbers, verbose)` — *(undocumented)*
- `_load_saved()` — Read back what a previous run wrote. Returns (frames, numbers).
- `run_step05(verbose, refit)` — Compute (or load) the raw quarterly descriptives.
- `_report(frames, numbers, suppressed)` — Human sanity check on stdout. NOT a source for any manuscript number.
- `main()` — *(undocumented)*

### `step06_sleep_measure_correlates.py`

Step 06 — Correlates of the quarterly sleep-quality item (Figure S3, Section S4).

- `_first_existing(candidates, what)` — The first path that exists, or a FileNotFoundError naming every candidate.
- `_load_inputs(verbose)` — Read every input and return (wide, dd, rep, ids).
- `_person_level_values(wide, dd, ids, verbose)` — Every baseline measure as a person-level Series, plus its print metadata.
- `_sweep(series, meta, rep, verbose)` — Correlate every baseline measure with the quarterly item, per quarter and overall.
- `_assemble_grid(mean_table, per_quarter, verbose)` — The sorted 17 x 12 grid, tidy, with an explicit integer `rank`.
- `pivot_grid(grid)` — Tidy grid -> (r, p, n) frames in RANK order, columns COL_LABELS.
- `check_grid(r, p, n)` — Everything that must be true of the grid before anything is published from it.
- `compute_numbers(grid, per_quarter, stopbang, verbose)` — Every quantity the documents quote, from the saved outputs alone.
- `_stored_numbers()` — The previous run's numbers.json, unprefixed, or {} if there is none.
- `run_step06(verbose, refit)` — *(undocumented)*
- `main()` — *(undocumented)*

### `step07_fit_coupling_model.py`

Step 04 — Fit the Bayesian VARX(1) coupling model + LOO-CV.

- `load_data(csv_path)` — Thin wrapper around lib.coupling_model.load_varx_frame.
- `_generate_coupling_figure(person_df, pop_mean, pop_ci_lo, pop_ci_hi, col_mean, col_ci_lo, col_ci_hi, col_prob, direction_label, out_path, prob_neg)` — Draw a 2-panel coupling figure (boxstrip + forest) and save.
- `run_step07(verbose, refit)` — Fit the coupling model and LOO-CV, produce all Step 03 outputs.
  - `_t(metric, value, note)` — *(undocumented)*
- `main()` — *(undocumented)*

### `step08_posterior_predictive_check.py`

Step 08 — Posterior predictive check of the primary coupling model (Table S5, Section S5).

- `_key_stem(variable, statistic)` — Registry key stem for one table row, e.g. 'pain_kurt', 'joint_corr_innov'.
- `numbers_from_table(table, meta)` — Every quantity Table S5 and Section S5 quote, under a stable key.
- `_stat_matrix(reps)` — (n_rep, 6) matrix of the canonical marginal statistics, replicate by replicate.
- `_check_arrays_match_table(table, stat_pain, stat_sleep, rep_corr, rep_ic)` — The saved arrays must be the arrays the table was scored from.
  - `_want(variable, statistic)` — *(undocumented)*
- `load_inputs(verbose)` — The model frame the primary fit used, and the posterior it produced.
- `simulate_and_score(md, idata, innovations, n_rep, seed, verbose)` — Table S5 plus everything a later figure would need, from one posterior.
- `_save(table, arrays, meta)` — *(undocumented)*
- `_load_saved()` — The table and the run metadata from a previous run; None if incomplete.
- `run_step08(verbose, refit, n_rep, seed)` — Score the primary model's posterior predictive check.
- `main()` — *(undocumented)*

### `step09_interpolation_sensitivity.py`

Step 09 — Sensitivity of the coupling estimates to interpolated observations.

- `flag_interpolated_transitions(df_full, model_df)` — Audit every transition for interpolated endpoints and return the clean subset.
- `_conform(df, arm, n_obs, n_persons)` — Force a summary frame onto SUMMARY_COLUMNS so both arms share one schema.
- `_lookup_number(numbers, name)` — Read one value out of a registry numbers.json, whatever prefix it carries.
- `primary_counts(model_df, unique_ids, verbose)` — The primary fit's transition and person counts, cross-checked against step 07.
- `load_primary_arm(n_obs, n_persons, verbose)` — The primary fit's posterior summary, from step 07's published outputs.
- `fit_nointerp_arm(clean, clean_ids, verbose)` — Fit the PRIMARY specification to the interpolation-free transitions.
- `_cell(summary, arm, param)` — One parameter's row for one arm, or None if the arm does not carry it.
- `estimate_numbers(summary, verbose)` — Every Table S7 cell, plus the two derived ratios the supplement states.
- `diagnostic_numbers(verbose)` — The interpolation-free fit's diagnostics, as `run_fit` recorded them.
- `count_numbers(flags, n_obs, n_persons, n_interp_rows)` — The 'X of Y transitions' block of Section S7, all of it derived, none hardcoded.
- `_saved_counts()` — The count block of a previous run, read back from its numbers.json.
- `write_table_s7(summary, path)` — Table S7 DATA: one row per parameter, the two arms side by side.
- `plot_arms(table, out_path)` — Forest plot of the two arms, drawn from the table DATA only.
- `run_step09(verbose, refit)` — Compare the primary fit with its interpolation-free refit (Table S7).
- `main()` — *(undocumented)*

### `step10_timevarying_covariates.py`

Step 10 — Time-varying covariates: fatigue/mood and treatment activity.

- `resolve_raw_long()` — First existing path in RAW_LONG_CANDIDATES.
- `_cols(block, timing)` — Covariate column names for one block at one timing.
- `_stem_parts(stem)` — ('fatigue_within_lag1') -> ('fatigue', '_lag1');  ('mood_within') -> ('mood', '').
- `_theta_key(block_key, model, equation, stem)` — numbers.json key for one theta, e.g. fm_C1_theta_pain_fatigue_lag1.
- `_row(summary, cov_set, model, param)` — One row of the tidy summary, or None if that parameter is not in the fit.
- `build_design(model_df, df_full, raw_long, verbose)` — Attach every block's covariates to the primary frame, once.
- `block_sample(model_df, design, block)` — The transitions, ids and rebuilt person index for one block's three fits.
- `fit_blocks(model_df, design, verbose)` — Fit M0/C1/C2 and M0/T1/T2 and return one tidy posterior summary.
- `collect_diagnostics(verbose)` — The six per-fit diagnostics records lib.run_fit wrote, as one frame.
- `panel_a(summary)` — Panel A: coupling and innovation correlation under each of the six models.
- `panel_b(summary)` — Panel B: the sixteen covariate coefficients (theta), pain and sleep equations.
- `endorsement_counts(raw_long_df, block)` — How often a binary item was endorsed, on the RAW (uninterpolated) column.
- `assemble_numbers(summary, design, raw_long_df, diagnostics)` — Every quantity Table S6 and Section S6 quote, under a stable key.
- `run_step10(verbose, refit)` — Rebuild Table S6 and numbers.json; ``refit`` re-runs the six fits.
- `main()` — *(undocumented)*

### `step11_contrast_moderation.py`

Step 04 — Contrast moderation analysis (Johnson-Neyman).

- `compute_simple_slopes(intercept_draws, slope_draws, x_positions)` — Compute posterior simple slopes at specified moderator values.
- `draw_jn_panel(ax, jn, panel_label, direction_label, slopes_dict, level_labels, level_x_vals, xlabel, body_knee_labels, legend_loc, info_loc, person_dots)` — Draw a JN panel with full annotations (person dots, rug, labels, info box).
- `run_step11(verbose, refit)` — Run contrast moderation JN analysis.
  - `_first_boundary(jn_result)` — *(undocumented)*
  - `_t(metric, value, note)` — *(undocumented)*
- `main()` — *(undocumented)*

### `step12_estimate_fmri_contrasts.py`

Step 05 — Re-estimate first-level fMRI contrasts (masked and unmasked).

- `_load_spm(spm_path)` — Load SPM.mat and return a flat dict of the key arrays.
- `_find_4d(subj_id)` — Find the 4D NIfTI for a subject in the BIDS fmri_4d tree.
- `_save_vol(data, affine, header, path)` — *(undocumented)*
- `process_subject(subj_id, overwrite, verbose)` — Re-estimate contrasts (masked + unmasked) for one subject.
- `run_step12(overwrite, verbose, refit)` — *(undocumented)*
- `main()` — *(undocumented)*

### `step13_extract_sp_rois.py`

Step 07 — Extract Sleep-to-Pain fMRI ROI values + Figure S4.

- `build_spherical_mask(mni_center, radius_mm, affine, shape)` — Create a boolean 3D mask for a sphere at MNI coordinates.
- `load_stim_side_map(verbose)` — Return {ID -> 'left' or 'right'} with the hemisphere CONTRALATERAL to
- `extract_rois(verbose)` — Extract fMRI BOLD in all SP ROIs and save to CSV.
- `_make_solid_cmap(hex_color)` — *(undocumented)*
- `_make_sphere_img(template, mni_center, radius_mm)` — *(undocumented)*
- `_render_roi_to_image(roi_img, template, cut_coords, title, cmap)` — *(undocumented)*
- `_make_composite(panels, ncols)` — *(undocumented)*
- `generate_figure_s4(verbose)` — Generate Figure S4: orthogonal brain slices for all SP ROIs.
- `run_step13(verbose, refit)` — *(undocumented)*
- `main()` — *(undocumented)*

### `step14_fit_sp_moderation.py`

Step 08 — Fit Sleep-to-Pain moderation models (7 ROIs).

- `load_step03_data(csv_path)` — Thin wrapper around lib.coupling_model.load_varx_frame.
- `run_step14(verbose, refit)` — *(undocumented)*
  - `_t(metric, value, note)` — *(undocumented)*
- `main()` — *(undocumented)*

### `step15_imaging_qc.py`

Step 15 — Imaging quality control, and the nuisance-adjusted sleep-to-pain moderation.

- `corr_on(ids, map_a, map_b)` — Pearson correlation of two person-level maps over one ID set.
- `site_contrast(ids, roi_map, site_map)` — Welch t-test and Cohen's d of one ROI's values by scanner site.
- `scan_motion(spm_dir, verbose)` — Framewise displacement for every SPM.mat under `spm_dir`.
- `summarize(values, prefix, out)` — mean / SD / median / min / max of one column, under `prefix`.
- `absmax_other(per_roi, named)` — Largest |r| among the ROIs the text does NOT name individually.
- `run_step15(verbose, refit)` — Imaging QC descriptives + the 16 nuisance-adjusted moderation fits.
- `main()` — *(undocumented)*

### `step16_sp_moderation_jn.py`

Step 09 — Johnson-Neyman analysis for SP moderation ROIs.

- `draw_jn_panel(ax, jn, direction_label, slopes_dict, level_labels, level_x_vals, xlabel, legend_loc, info_loc, person_dots, left_label_inside)` — Draw a full JN panel with person dots, rug, annotated simple slopes,
- `run_step16(verbose, refit)` — *(undocumented)*
  - `_t(metric, value, note)` — *(undocumented)*
  - `_person_dots(roi_name)` — *(undocumented)*
- `main()` — *(undocumented)*

### `step17_ps_specificity.py`

Step 17 — Directional-specificity control for the sleep-to-pain fMRI ROIs.

- `_build_summary(fitted)` — Select and rename ``fit_roi_moderation_set``'s columns into the
- `_step14_n_persons()` — Number of persons step 14 fitted for these same four ROIs, or None.
- `_collect_numbers(summary, verbose)` — Every quantity this step publishes, under its registry key.
- `run_step17(verbose, refit)` — Fit (or reload) the four pain-to-sleep specificity fits and publish
- `main()` — *(undocumented)*

### `step18_extract_ps_rois.py`

Step 10 — Extract Pain-to-Sleep arousal relay ROI values + Figure S6.

- `extract_fmri_arousal(verbose)` — Extract probability-weighted mean BOLD per subject per ROI.
- `extract_vbm_arousal(verbose)` — Extract probability-weighted GM volume per subject per ROI.
- `_make_solid_cmap(hex_color)` — *(undocumented)*
- `_load_atlas_roi(roi_cfg, template)` — *(undocumented)*
- `_get_center_of_mass(roi_img)` — *(undocumented)*
- `_render_roi_to_image(roi_img, template, cut_coords, title, cmap)` — *(undocumented)*
- `_make_composite(panels, ncols)` — *(undocumented)*
- `generate_figure_s6(verbose)` — Generate Figure S6: orthogonal brain slices for all PS arousal ROIs.
- `run_step18(verbose, refit)` — *(undocumented)*
- `main()` — *(undocumented)*

### `step19_fit_ps_moderation.py`

Step 11 — Fit Pain-to-Sleep moderation models (arousal relay ROIs).

- `load_step03_data(csv_path)` — Thin wrapper around lib.coupling_model.load_varx_frame.
- `fit_modality(modality_name, roi_csv_path, model_df, unique_ids, id_map, verbose)` — Fit moderation models for all ROIs in one modality (fMRI or VBM).
- `run_step19(verbose, refit)` — *(undocumented)*
  - `_t(metric, value, note)` — *(undocumented)*
- `main()` — *(undocumented)*

### `step20_ps_moderation_jn.py`

Step 12 — Johnson-Neyman analysis for PS arousal moderation ROIs.

- `draw_jn_panel(ax, jn, direction_label, slopes_dict, level_labels, level_x_vals, xlabel, legend_loc, info_loc, person_dots, left_label_inside)` — Draw a full JN panel with person dots, rug, annotated simple slopes,
- `run_jn_for_modality(modality_name, draws_path, table_path, verbose)` — Run JN for all ROIs in one modality. Returns JN grid rows, text rows,
- `_person_dots_ps(d, roi_name)` — Compute person-level fitted PS coupling values for scatter overlay.
- `_make_merged_figure(jn_results, slopes_all, d, table, modality_label, fig_path, fig_title, verbose)` — Generate merged multi-panel JN figure for one modality.
- `run_step20(verbose, refit)` — *(undocumented)*
- `main()` — *(undocumented)*

### `step21_severity_moderation.py`

Step 13 — Person-mean severity moderation of coupling (Table S2).

- `load_data(csv_path)` — Thin wrapper around lib.coupling_model.load_varx_frame.
- `run_step21(verbose, refit)` — *(undocumented)*
  - `_t(metric, value, note)` — *(undocumented)*
- `_fit_joint_model(model_df, unique_ids, id_map, pain_mean, pain_sd, sleep_mean, sleep_sd, table_rows, text_rows, verbose, fit_id, out_dir)` — Fit a 2-moderator VARX(1) model with both pain and sleep severity.
  - `_summarize(var_name)` — *(undocumented)*
- `main()` — *(undocumented)*

### `step22_diagnostics_summary.py`

Step 22 — Collect the diagnostics of every fit in the paper.

- `collect(verbose)` — Every per-fit diagnostics record the pipeline wrote.
- `by_family(verbose)` — Table S3: convergence by parameter family, for the primary model.
- `run_step22(verbose, refit)` — *(undocumented)*
- `main()` — *(undocumented)*

### `step23_optimal_lag.py`

Step 23 — The optimal measurement interval implied by the fitted coupling matrix.

- `read_transition_matrix(path)` — The four coefficients of A, from the primary fit's published Table 4.
- `eigenvalues(phi_p, phi_s, lambda_sp, lambda_ps)` — (lambda_1, lambda_2, discriminant) of the bivariate transition matrix.
- `amplification(omega, phi_p, phi_s)` — f(omega), the cross-lagged amplification at lag omega, normalized so f(1) = 1.
- `unidirectional_optimum(phi_p, phi_s)` — Eq. S3's omega_opt, in quarters; NaN when the logarithms do not exist.
- `derive(coeffs, verbose)` — Every quantity Section S14 reports. Returns (tidy DataFrame, numbers dict).
- `run_step23(verbose, refit)` — Derive Section S14's optimal-lag quantities from the primary fit.
- `main()` — *(undocumented)*

## Tools — `tools/`

### `check_numbers.py`

Verify that every number in the documents traces back to a pipeline output.

- `load_pool()` — Every value the pipeline produced: the registry, plus all numeric CSV cells.
- `whitelist()` — Numbers that legitimately have no pipeline source, with the reason recorded.
- `matches(value, text, pool, tol_digits)` — True when some pooled value rounds to `value` at the document's precision.
- `decimals(token)` — *(undocumented)*
- `main()` — *(undocumented)*

### `compare_interpolation_arms.py`

Compare the two interpolation arms on everything the paper concludes.

- `credible(lo, hi)` — *(undocumented)*
- `load(root_results, root_deriv, rel)` — *(undocumented)*
- `table4(res, der)` — *(undocumented)*
- `section(title)` — *(undocumented)*
- `main()` — *(undocumented)*

### `make_functions_md.py`

Regenerate `codes/python/FUNCTIONS.md` from the code itself.

- `summarize(node)` — First docstring line of a callable, or a visible placeholder.
- `signature(node)` — `(a, b, *rest, kw, **kwargs)` — names only; defaults are in the source.
- `walk(node, depth, lines)` — Emit one bullet per callable, indented by nesting depth.
- `render()` — The whole registry as one Markdown string.
- `main()` — *(undocumented)*

### `number_diff_report.py`

What every reported number is now, and what the regenerated pipeline says it should be.

- `table4()` — *(undocumented)*
- `main()` — *(undocumented)*

### `renumber_pipeline.py`

Renumber the pipeline from 13 steps to 23, in one sentinel-protected pass.

- `moved(old)` — True when a number actually changes; identity entries are left alone.
- `rewrite_text(path, dry)` — Sentinel-protected rewrite of every `stepNN` token in one file.
- `git_mv(src, dst, dry)` — *(undocumented)*
- `main()` — *(undocumented)*

### `strip_prose_generation.py`

Remove narrative generation from every pipeline step (Decision 3).

- `strip_file(path, dry)` — Delete the generator definition and every call to it. Returns (lines, calls).
- `main()` — *(undocumented)*

### `update_figures.py`

Replace every figure in the submission with the one the pipeline just produced.

- `update_manuscript_tiffs(dry)` — Convert each regenerated PNG to the TIFF the journal receives.
- `update_supplement(dry)` — Swap both stored copies of each embedded figure, guarded by its caption.
- `main()` — *(undocumented)*

### `verify_code_vs_docs.py`

Verify code-generated text matches the documents.

- class `Paragraph` — *(undocumented)*
- `normalize(s)` — *(undocumented)*
- `split_paragraphs(text)` — *(undocumented)*
- `classify_kind(p)` — *(undocumented)*
- `cites_numbers(p)` — True if paragraph reports *computed* results (not method parameters).
- `load_paragraphs(path)` — *(undocumented)*
- `is_narrative_only(p)` — True if paragraph is creative prose not backed by code outputs.
- `best_match(target, candidates)` — *(undocumented)*
- `short(s, n)` — *(undocumented)*
- `run_verification(verbose)` — One-way check: for every code-generated paragraph, find its closest
- `main()` — *(undocumented)*

### `verify_refit_reproduces.py`

Did the re-run reproduce the results it was supposed to reproduce?

- `tabular(root)` — {relative path: absolute path} for every CSV under `root`.
- `compare(a_path, b_path)` — (verdict, detail) for one pair of CSVs.
  - `numeric(s)` — *(undocumented)*
- `main()` — *(undocumented)*

